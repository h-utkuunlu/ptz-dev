cmake_minimum_required(VERSION 3.4)
project( camera )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( camera color_tracker.cpp )
target_link_libraries( camera ${OpenCV_LIBS} )
